# goreleaser.yml
# Build customization
build:
  binary_name: coverpr
  goos:
    - windows
    - darwin
    - linux
  goarch:
    - amd64


brew:
  # Reporitory to push the tap to.
  repo: sorenmat/homebrew-tap

  # Folder inside the repository to put the formula.
  # Default is the root folder.
  folder: Formula
